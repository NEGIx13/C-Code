#include <stdio.h>
#include <string.h>
#include <math.h>

int main(){
    enum day{
        monday, tuesday, wednesday, thruday, friday, saturday, sunday
    };
    enum day negi;
    printf("%d", negi = 1);
//    printf("%d", negi.1);
    return 0;
}
