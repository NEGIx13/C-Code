#include <stdio.h>
#include <math.h>

int main(){
    int b,c;
    b = 0;
    c = 1;
    // c++;
    // b++;
    int a = b + c;
    int power = pow(2,3);
    int d = b^c;
    int m = b%2
    printf("%d %d", power, d);
}